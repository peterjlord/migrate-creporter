name: Migrate CReporter
type: module
description: Migrate Community Reporter Site
core: 8.x
package: CReporter
dependencies:
  - migrate_plus
  - migrate_tools
  - migrate
  - migrate_upgrade

config_devel:
 install:
   - migrate_plus.migration.creporter.categories
   - migrate_plus.migration.creporter.tags
   - migrate_plus.migration.creporter.locations
   - migrate_plus.migration_group.creporter
